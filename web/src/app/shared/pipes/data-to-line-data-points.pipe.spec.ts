import { DataToLineDataPointsPipe } from './data-to-line-data-points.pipe';

describe('DataToLineDataPointsPipe', () => {
  it('create an instance', () => {
    const pipe = new DataToLineDataPointsPipe();
    expect(pipe).toBeTruthy();
  });
});
