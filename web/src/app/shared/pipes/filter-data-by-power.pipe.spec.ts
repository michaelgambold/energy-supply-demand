import { FilterDataByPowerPipe } from './filter-data-by-power.pipe';

describe('FilterDataByPowerPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDataByPowerPipe();
    expect(pipe).toBeTruthy();
  });
});
