import { RegionsToStackedBarCategoriesPipe } from './regions-to-stacked-bar-categories';

describe('RegionsToStackedBarCategoriesPipe', () => {
  it('create an instance', () => {
    const pipe = new RegionsToStackedBarCategoriesPipe();
    expect(pipe).toBeTruthy();
  });
});
