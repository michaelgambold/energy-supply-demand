import { FilterDataByFuelPipe } from './filter-data-by-fuel.pipe';

describe('FilterDataByFuelPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDataByFuelPipe();
    expect(pipe).toBeTruthy();
  });
});
