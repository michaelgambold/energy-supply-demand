import { DataToGreenFossilLineDataPointsPipe } from './data-to-green-fossil-line-data-points.pipe';

describe('DataToGreenFossilLineDataPointsPipe', () => {
  it('create an instance', () => {
    const pipe = new DataToGreenFossilLineDataPointsPipe();
    expect(pipe).toBeTruthy();
  });
});
