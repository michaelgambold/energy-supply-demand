import { FilterDataByRegionPipe } from './filter-data-by-region.pipe';

describe('FilterDataByRegionPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDataByRegionPipe();
    expect(pipe).toBeTruthy();
  });
});
