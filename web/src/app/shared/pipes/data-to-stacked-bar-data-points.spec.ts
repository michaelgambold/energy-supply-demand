import { DataToStackedBarDataPointsPipe } from './data-to-stacked-bar-data-points.pipe';

describe('DataToStackedBarDataPointsPipe', () => {
  it('create an instance', () => {
    const pipe = new DataToStackedBarDataPointsPipe();
    expect(pipe).toBeTruthy();
  });
});
