import { FuelsToStackedBarSeriesPipe } from './fuels-to-stacked-bar-series.pipe';

describe('FuelsToStackedBarSeriesPipe', () => {
  it('create an instance', () => {
    const pipe = new FuelsToStackedBarSeriesPipe();
    expect(pipe).toBeTruthy();
  });
});
