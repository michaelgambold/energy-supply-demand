import { FuelsToLineSeriesPipe } from './fuels-to-line-series.pipe';

describe('FuelsToLineSeriesPipe', () => {
  it('create an instance', () => {
    const pipe = new FuelsToLineSeriesPipe();
    expect(pipe).toBeTruthy();
  });
});
